#include "main.h"
#include <stdio.h>

/**
  *_strlen-function that returns the length of a string
  *@s-the variable containing the string
  *
  */
int _strlen(char *s)
{
	_printf("%d\n", _strlen(*s));

}
